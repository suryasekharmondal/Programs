#include <stdio.h>
#include <stdlib.h>
#include <math.h>

long long int mult(long long int numA, long long int numB, int m, int n){
	
	
}
int main(){
	int test;
	printf("Enter the number of times you want to multiply: ");
	scanf("%d", &test);
	while(t--){
		
		printf("Enter two numbers: ");
		
		scanf("%lld %lld", &numA, &numB);
		
		storeA= numA;
		storeB= numB;
		while(storeA!=0){
			m++;
			storeA/=10;
		}
		
		while(storeB!=0){
			n++;
			storeB/=10;
		}
		
	}
}
